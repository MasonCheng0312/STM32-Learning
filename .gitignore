# -----------------------------
# 1. 編譯產生的垃圾 (必須忽略)
# -----------------------------
build/
Debug/
Release/
*.elf
*.bin
*.hex
*.map
*.o
*.d
*.su

# -----------------------------
# 2. 編輯器暫存與快取 (必須忽略)
# -----------------------------
.clangd
.vscode/ipch/
.settings/
*.code-workspace
.DS_Store
Thumbs.db

# -----------------------------
# 3. CubeMX 相關 (選擇性忽略，建議忽略)
# -----------------------------
.mxproject
*.tmp
*.bak

# -----------------------------
# 4. 特別注意：不要忽略這些
# -----------------------------
# !*.ioc    <-- 這是註解提醒你，千萬別把 ioc 擋掉
# !*.ld     <-- 千萬別把 ld 擋掉
# !*.s      <-- 千萬別把 s 擋掉